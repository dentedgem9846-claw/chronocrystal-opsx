```
ROUTED TO KIMI:
(none)

DOCUMENTATION UPDATED:
- `specs/kawa-message-sender/spec.md`:
  - Fixed type mismatch W2: changed `startLiveMessage` return type from `{ itemId: string } | null` to `{ itemId: number } | null` to match the code and SimpleX SDK.
  - Fixed spec contradiction W1: rewrote the "No send-or-update indirection" requirement to state callers SHALL use the public `updateLiveMessage` method (which encapsulates the null guard and delegates to `updateLiveMessageCmd`). Removed the contradictory "Callers invoke updateLiveMessageCmd directly" scenario.
- `tasks.md`:
  - Updated task 3.2 to reflect that callers use the public `updateLiveMessage` method rather than calling `updateLiveMessageCmd` directly (S1).
  - Updated task 7.1 to accurately state that no unit test files exist for this module and `vitest run --exclude tests/e2e` exits with "No test files found" (S2).

FILED AS ISSUES (scope increase — not for this cycle):
ISSUE: Add unit tests for MessageSender — Cover null-guard skip paths in `updateLiveMessage` and `finalizeLiveMessage`, `startLiveMessage` null return on failure, and `sendTextMessage` error handling.
  Source: flux/kawa/tests/ contains only e2e tests
  Scope: missing spec

ISSUE: Enforce MessageSender API boundary by making `updateLiveMessageCmd` private — Prevents external callers from bypassing the null guard in `updateLiveMessage`. Requires verifying no external callers exist (confirmed: none in `kawa.ts` or elsewhere).
  Source: message-sender.ts:82
  Scope: architectural change

ASSESSMENT: DOCS_ONLY
```
