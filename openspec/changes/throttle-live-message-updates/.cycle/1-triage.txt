ROUTED TO KIMI:

WARNING: flux/kawa/src/live-message-throttler.ts:60 — `flush()` sends an `updateLiveMessage` call even when no throttle timer is pending and no text has changed since the last send. Spec scenario "Flush with no pending timer" says: "the method SHALL be a no-op (no update sent if no buffered text has changed since last send)". The implementation always sends when `liveMessageState === "STREAMING"` without tracking whether text has changed since the last send. This contradicts the spec and is in scope because the throttle spec explicitly requires no-op behavior for redundant flush calls.

WARNING: flux/kawa/src/live-message-throttler.ts:73-76 — `LiveMessageThrottler.cancel()` method exists but is never called. Timer cleanup is done inline in `session-manager.ts:60-62`, `kawa.ts:564-566`, and `commands.ts:63-65`. Task 1.10 says "call throttler's `cancel()`" and task 1.11 says the same. The behavior is functionally equivalent, but the uncalled method is dead code that contradicts the tasks. Kimi should either replace the three inline cleanup sites with `throttler.cancel()` calls or remove the dead `cancel()` method.

DOCUMENTATION UPDATED:
- `specs/markdown-to-simplex/spec.md`: Updated "Preserving SimpleX-native formatting" requirement to document the known limitation that regex cannot distinguish `*bold*` from `*italic*`, and standard markdown interpretation takes precedence in ambiguous cases. Updated the "Already-correct SimpleX bold" scenario with a note about this limitation.
- `tasks.md`: Updated task 3.11 to reflect that `_italic_` and `~strike~` pass through unchanged, while `*bold*` is an ambiguous case treated as standard italic per conversion rules.
- `design.md`: Updated the "Race between timer fire and flush" row in Risks/Trade-offs to describe the actual implementation behavior (JS single-threaded event loop makes explicit null check unnecessary) rather than a check that doesn't exist.

FILED AS ISSUES (scope increase — not for this cycle):
None.

ASSESSMENT: HAS_CODE_FIXES
