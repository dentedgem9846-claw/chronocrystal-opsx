```
ROUTED TO KIMI:
CRITICAL: flux/kawa/src/message-sender.ts:35 — `updateLiveMessage(ctx, text)` accepts a `text` parameter but ignores it, always using `ctx.accumulatedText` instead. The spec's non-goal is preserving the `MessageSender` public API, so fix the method body to use the `text` parameter (or remove the parameter if callers always pass `ctx.accumulatedText`). Already filed in issues.md.

CRITICAL: flux/kawa/src/session-manager.ts:92 — `closeAll()` does not cancel throttle timers via `this.throttler?.cancel()`. `removeByContactId` correctly cancels timers, but `closeAll` only calls `unsubscribe()`. This is a cleanup gap for session teardown that contradicts the design goal of preventing timer leaks. Add `this.throttler?.cancel(ctx)` before `ctx.unsubscribe?.()` in the `closeAll` loop.

WARNING: flux/kawa/src/commands.ts:52 — `/help` command text uses standard markdown `**Kawa Commands**` instead of SimpleX markdown `*Kawa Commands*`. Since `sendTextMessage` does not go through `EventFormatter.extractMessageText`, this renders as raw `**Kawa Commands**` in SimpleX Chat. Fix the hardcoded strings to use SimpleX dialect.

WARNING: flux/kawa/src/commands.ts:122 — `/status` command text uses standard markdown `**Session Status**` instead of SimpleX markdown `*Session Status*`. Same issue as `/help` — fix the hardcoded strings.

WARNING: flux/kawa/tests/markdown-to-simplex.test.ts:139 — The test "preserves already-correct SimpleX bold *text*" has no assertions (only comments). The spec now states that `*text*` is treated as standard italic and converted to `_text_`. Rewrite the test with a proper assertion, or remove it if it no longer tests a valid spec requirement.

DOCUMENTATION UPDATED:
- specs/markdown-to-simplex/spec.md: Changed "Already-correct SimpleX bold" scenario to "Ambiguous single-asterisk text" — the old SHALL stated `*correct bold*` SHALL pass through unchanged, which contradicted the implementation. The new scenario documents the actual behavior (`*text*` → `_text_`) and retains the NOTE explaining why.
- tasks.md: Fixed task 1.11 description from "cancel before incrementing generation" to "cancel around the generation increment" to match the actual code in `kawa.ts` where `generation++` executes synchronously before `throttler.cancel(ctx)`.
- design.md: Fixed Decision 7 — corrected `discoverSystemPromptFile()` path from `<agentDir>/.pi/SYSTEM.md` to `<agentDir>/SYSTEM.md`.

FILED AS ISSUES (scope increase — not for this cycle):
None.

ASSESSMENT: HAS_CODE_FIXES
```
