ROUTED TO KIMI:
WARNING: flux/kawa/src/kawa.ts:162 — Error handler in `wireSessionEvents` does not cancel the throttle timer when `handleAgentEvent` throws. The catch handler resets `liveMessageState` to `"IDLE"`, `liveMessageItemId` to `null`, and `accumulatedText` to `""`, but omits `throttler.cancel(ctx)` and `ctx.lastSentText = ""`. A pending timer will fire after error recovery, producing a spurious warning log (`No live message item ID for contact X, skipping update`). This is in scope because proper timer cleanup is required by the throttler integration (analogous to the session reset paths already specified in tasks 1.10–1.11).

SUGGESTION: flux/kawa/src/message-sender.ts:52 — `finalizeLiveMessage` resets `accumulatedText`, `liveMessageState`, and `liveMessageItemId` but not `lastSentText`. Add `ctx.lastSentText = ""` after the existing resets for defensive consistency, since `lastSentText` was introduced by this change as throttle state.

DOCUMENTATION UPDATED:
- None needed.

FILED AS ISSUES (scope increase — not for this cycle):
- None. (The previously filed `MessageSender.updateLiveMessage ignores its text parameter` issue remains in `issues.md` and is not re-filed.)

ASSESSMENT: HAS_CODE_FIXES
